#!/bin/bash
#
# PR News - LLM Module (Claude CLI)
# =================================
#
# Claude CLI를 래핑하여 PR 요약을 생성합니다.
#
# Functions:
#   llm::summarize - PR 데이터를 분석하여 요약 생성
#   llm::check     - Claude CLI 사용 가능 여부 확인
#
# Output Sections:
#   📦 주요 변경사항 - 새 기능, 개선, 리팩토링
#   🐛 버그 수정     - 버그 픽스 (있는 경우)
#   💡 학습 포인트   - 리뷰 코멘트에서 얻은 인사이트
#   ⚠️ 주의사항      - breaking changes 등 (있는 경우)
#
# Dependencies:
#   - claude (Claude CLI)
#

# System prompt for PR summarization
readonly LLM_SYSTEM_PROMPT='당신은 GitHub PR 변경사항을 분석하여 팀원이 따라잡아야 할 핵심 내용을 요약하는 역할입니다.

분석 관점:
1. 주요 기능 추가/변경사항
2. 중요한 기술적 결정 및 아키텍처 변경
3. 버그 수정 및 개선사항
4. 팀 리뷰에서 나온 피드백 및 학습 포인트
5. 컨트리뷰터가 알아야 할 코드 패턴/컨벤션

출력 형식:
- 한글로 작성
- 간결하고 실용적으로
- bullet points 사용
- 핵심만 추출 (장황하게 X)'

# Generate summary using Claude CLI
llm::summarize() {
  local pr_data="$1"
  local repo="$2"
  local pr_count="$3"

  local user_prompt="다음은 ${repo} 레포지토리의 최근 머지된 PR ${pr_count}개입니다.

컨트리뷰터로서 따라잡아야 할 핵심 내용을 요약해주세요.

---
${pr_data}
---

위 PR들을 분석하여 다음 섹션으로 요약해주세요:

## 📦 주요 변경사항
(새 기능, 개선, 리팩토링 등)

## 🐛 버그 수정
(있는 경우만)

## 💡 학습 포인트
(리뷰 코멘트에서 얻은 인사이트, 코드 패턴 등)

## ⚠️ 주의사항
(breaking changes, 마이그레이션 필요 등 - 있는 경우만)"

  echo "$user_prompt" | claude -p --system-prompt "$LLM_SYSTEM_PROMPT" 2>/dev/null
}

# Check Claude CLI availability
llm::check() {
  if ! command -v claude &>/dev/null; then
    return 1
  fi
  return 0
}
